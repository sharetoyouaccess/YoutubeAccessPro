# Byte-compiled / cache
__pycache__/
*.pyc
*.pyo

# Vendor binaries - never commit these (too large for git, not this project's code).
# scripts/build_addon.py pulls them from a known-good .nvda-addon at build time.
globalPlugins/lib/ffmpeg/
globalPlugins/lib/mpv/

# AI karaoke engine binary + model - also too large for git (100MB+ each).
# Kept locally on the dev machine only; scripts/build_addon.py reads it
# fresh from this path at build time, never committed.
globalPlugins/lib/uvr/

# Build output
*.nvda-addon
/build/
/dist/

# OS/editor cruft
.DS_Store
Thumbs.db
*.swp
